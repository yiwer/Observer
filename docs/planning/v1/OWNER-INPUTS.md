# Owner 最新运行授权与服务选择

更新日期：2026-09-08（Asia/Shanghai）。本文件记录已确认输入；它不代表这些能力已实际通过。较早票据中的外部未授权状态应结合本记录判断；来源许可、安全和真实生产验收保持，国家/地区审查已按下述最新指令取消。

## 最新决定：native Codex 与取消国家审查

### 同日后续覆盖：六栏 HTML 更新稿与来源接入

Owner 随后进一步要求：**每个栏目一封独立邮件**，只要“当前日期减一天的00:00至整理日报的采集点”范围内发布的信息；事件以前发生、但窗口内才报道的可收录。沿用北京时间 Asia/Shanghai。这个要求覆盖此前允许本周回顾、未知发布日期的今日热榜或仓库快照的宽松规则；今天观测到不等于窗口内发布。不得为了凑条数回退旧窗口。本机手动六封分栏更新稿是此次执行范围，不自动启用未来定时邮件。

Owner 又明确增加第二个QQ订阅邮箱并保留原地址。私有收件配置记录两个稳定订阅者ID，每栏分别发给两人，因此本轮是12封独立邮件；发件人仍为原QQ邮箱，SMTP授权码不变。不在公共文档/Issue暴露具体地址，也不向其他收件人公开订阅列表。

Owner 在收到旧补发版后要求直接提供六栏新闻内容，改用 HTML，不以每栏七条为门槛；随后提供 Tavily、Exa、OpenAlex、知乎、Alpha Vantage 凭据并要求配置、检查实际权限额度、按来源研究重新构建流程，再生成今日六栏邮件。本次因此允许必要的小量真实数据请求和一次明确的 HTML 更新稿邮件，不要求 MD/PDF 附件、不采购或充值、不调用 Claude，也不覆盖旧版或开启每日自动发送。密钥仅存在本机用户环境或进程内存，本文件不记录值及私人收件地址。

实施入口见[本机 HTML 流程](../../implementation/daily-html.md)。旧段落中的“仅五类最小来源”“本次必须 MD/PDF 附件”不再是这次明确更新稿的边界；第三方实际拒绝仍遵守，API 可用不表示无限转载或已获得全部平台用途许可。真实调用、生成条目与交付结果以本轮记录为准。

2026-09-08 Owner 已回复“确认授权”，批准 arXiv、Eurostat、美联储、USGS、GitHub 的最少公开元数据用于私人日报摘要与邮件附件，范围见 [starter](../../research/today-source-starter-2026-09-08.md)。可据此配置真实白名单，不再等待同一授权；不包含论文全文、图片、README/源码、Release 正文或个人帖子。社交来源缺口保留，GitHub 匿名读取接线与真实内容结果另记。

Owner 已明确回复“确认采用本机已登录 Codex”，随后要求“跳过国家审查”。首轮改走本机 native CLI，由 CLI 自身使用已有登录，不复制/输出认证文件，不要求额外 API key 或新增 API 计费。国家/地区信息不再是项目测试、开发或接入的前置输入，不再询问，不把跳过标为资格审查通过；见 [ADR-0006](../../adr/0006-use-native-codex-for-first-local-validation.md)。

此前本机 Codex 真实测试、不设额度上限的授权继续有效，可以在入口实现后作一次必要有界真实调用；无需重新请求国家信息或调用授权。保留进程超时/取消、秘密保护和准确执行元数据，不修改全局网络/认证设置，不绕过实际服务拒绝。Claude 真实测试仍跳过，目标云部署、节点外存储和真实来源权利仍各自处理，但不阻止 native 入口实现与本机最小调用验证。

## V1 快速交付策略（覆盖旧测试流程）

Owner 明确要求快速落地、快速验证：V1可以大幅跳过测试、夹具和回归；不要做hash验证，不要走一小步就回归。自#14当前WIP起立即执行，后续票沿用。

- 按完整功能块实现并提交，不再逐切片capture、hash取证或反复回归；停止为覆盖矩阵扩充夹具、容量与边界变体。
- 保留简短代码review，必要时在功能块结束后做一次类型检查或启动/关键路径验证；不默认全量测试、独立兼容矩阵或多轮集成复验。
- 未测范围简要记录，不作为V1开发票推进门槛；已知会使核心功能无法使用的缺陷仍需处理，不能伪报通过。
- 此处取消的是开发验收中的hash验证，不是要求删除产品已有内容完整性功能。保留已有证据与未完成草稿，不为整理而删除用户数据。
- 外部调用/凭证/发送授权与真实上线结论仍按实际证据区分；开发票交付不等于生产已验证。

## Codex

2026-09-08 Owner 指定 Observer 调用 Codex 的默认模型为 **`gpt-6-astra`**，推理强度 **`medium`**；研究与语义复核统一使用。不静默降级到其他模型，不重写历史执行记录。官方[模型说明](https://developers.openai.com/api/docs/models/gpt-6-astra)列明支持 `medium`，但本机账户可用性以实际调用结果为准；native 订阅用量不按 API 价目虚构费用。

Owner 明确批准使用**本机已有 Codex** 做真实测试，**不设置额度上限**。这允许任务所需的模型消耗，不要求购买额外资源、修改全局配置或无限重试；PRD D7 的进程时限、调用轮数、取消、用量记录及防失控边界仍保留。

本机重新读取 `codex --version` 为 **0.153.4**，`codex login status` 为 **Logged in using ChatGPT**；只读取登录状态，未输出/复制认证文件、令牌或用户配置。官方[非交互认证文档](https://learn.chatgpt.com/docs/non-interactive-mode#authenticate-in-automation)说明 `codex exec` 可复用已保存的 CLI 认证。使用已有登录应由 CLI 自身处理，不能把认证复制到候选运行环境、公共仓库或日志中。

本机国家/地区审查已由 Owner 明确取消，不再阻断或追问；登录/调用结果只证明各自实际观察，不转写地区资格 PASS，也不证明 Linux 容器隔离。本机 native 实测、目标 VPS 测试及 14 天影子质量记录分别记录。最新只读刷新仍为 CLI 0.153.4、Logged in using ChatGPT；截至本次入口派发仅完成 version/help/login-status，**未发起真实模型请求**。

## 当前产品接入边界（代码事实，不是新增授权）

#23 既有容器入口由 `src/production-runtime.ts` 读取专用 `OBSERVER_OPENAI_API_KEY` / `OBSERVER_ANTHROPIC_API_KEY`（可用对应 `_FILE`），通过受控 API broker 服务真实 CLI；它不复制或复用本机个人 Codex 登录文件。Owner 现已选择另行接通 native 路径，方向不再待确认；该适配尚需实现，不把 native 结果冒充容器/API 路径通过，也不因旧代码需要 API key 而重新要求 Owner 购买。原容器路径保留准确边界，不能读取/转移 OAuth 或订阅凭证。

## Claude

Owner 将后续提供真实 Claude 环境，当前**跳过真实环境测试**并标为 `owner-deferred / NOT VERIFIED`。不再把当前缺少 Claude 真实凭证当成开发票的等待条件，但也不宣称双 Provider 实测通过。

这不跳过 #5 的离线适配器实现、协议替身测试、真实固定 CLI + 无凭证模型替身，以及已发现的 SSE/用量 P2 修复和双轴复审。任何实际 Claude 认证/模型调用继续禁用，直至 Owner 提供并授权适合的环境。T3 已允许按可用 Provider 记录单 Provider 范围，不把 Claude 缺项伪装成 PASS。

## GitHub 栏最新规则（2026-09-08）

Owner 明确“github热榜不需要限制发布时间，而是根据github的热门趋势榜单来决定”。当前本机六栏HTML流程以 GitHub 官方 Trending Today 全语言榜选题，记录榜单名次和本次观察时间；仓库创建、项目发布、Release日期都不是入选门槛。历史已报道项目仍降权，按仓库身份合并仓库页和Release链接的历史记录。榜单今日新增Star与总Star分开，不以总Star搜索替代Trending；源不可用明确提示，不静默使用旧榜。其他五栏仍只收北京时间前一天00:00至采集冻结点内发布的资料。本次规则更新不授权重发已交付邮件。

## 六栏影响解读与财经行情（2026-09-08 后续新增）

Owner 要求各栏增加“AI 一句话解读”，说明主要影响；本机 HTML 流程按每条新闻/项目必填一句实现，明确 AI 分析而非已证实因果，不以复述标题代替影响。财经额外展示布油、黄金、费城半导体、标普500、道琼斯工业、纳斯达克综合、韩国综合、日经225、上证综指、创业板指、恒生科技，共11项价格/点位、涨跌额、涨跌幅、来源、报价时点与简要影响解读。

实现默认布油为布伦特原油期货、黄金为 COMEX 黄金期货，不是现货；此口径为工程推荐默认而非 Owner 指定现货/合约。行情是独立最新报价快照，不冒充时间窗内的新报道，不占新闻条数。休市/跨时区仅标实际最近可得报价日期，不把旧交易日变化称今日涨跌；无法确认最新时直说，不从旧价格补算或让模型猜数字。数据源、口径和缺失逐项在邮件公示；新闻窗口及GitHub例外维持不变。本次只修改后续生成流程与作必要有界本地验证，不重发旧邮件、不启用自动投递、不采购或充值。

## 已授权每日定时投递（2026-09-08）

Owner 随后要求“设置定时任务，按照之前预设的时间”，明确授权未来每天自动采集、生成和发送六栏HTML邮件。当前本机Windows实现：Asia/Shanghai每天07:30启动，新闻发布时间固定截止07:30，生成后立即按各栏分别发送给私有配置中的全部订阅者，目标08:30前SMTP受理；这不冒称用户实际收件或旧PRD私有服务器已可读。GitHub和行情仍保留各自真实采集时点。

使用当前Windows用户Interactive任务以复用本机登录，要求主机开机/联网且该用户保持登录（锁屏可），请求唤醒但不保证硬件电源策略允许；不存Windows密码、不改网络配置。错过08:30不自动晚间补发。固定当日日志/运行声明与SMTP尝试防止重入和未知状态重复发送；部分栏失败时其它栏继续，该栏显式告知未完成。此授权覆盖前文“未开启未来自动投递”的历史边界，但不授权重发已完成的旧run。安装/首次运行状态见 [本机定时任务](../../implementation/daily-schedule.md)。

## 邮件

2026-09-08 Owner 要求本机落地后，向已确认的私人邮箱发送当天各类日报输出。此为真实发送授权，不是再次预检。当天自动送达与午前补齐时间已过，因此本次必须标明 Owner 触发的手动补发、实际采集/截稿时间；不得伪报按时定时日报。输出包含六个 Edition（无足够真实来源时明确缺口）、以 Canonical MD 派生的 HTML 和 PDF，并附完整 MD/PDF。由于当前未配置公网 HTTPS 归档，此次采用附件模式，不构造虚假下载链接；不因此自动启用每日调度或重复发送。SMTP accepted 与实际收件仍分开记录，unknown 不盲目重发。

Owner 选择 **QQ 邮箱 SMTP + 授权码**，并确认发件与收件使用同一邮箱。#19 的传输要求因此由仅 HTTPS 修订为可替换邮件传输适配器、首个实现采用 SMTP/TLS；具体端口、TLS 模式、附件预算与提供商能力在实施时核对官方资料和实际环境。

仓库 `yiwer/Observer` 已读回 **PUBLIC**，因此具体邮箱地址仅存于 Git 忽略的本地 `data/operator/owner-inputs.json`，不写入公共 Issue、提交或研究文档。该文件是运行输入交接记录，**不是已经生效的产品配置**；不得把授权码填进这个非秘密字段集合。Owner 已将授权码存入 Windows 用户级环境变量 `QQ_SMTP_KEY` 并明确授权调用及测试；Root 仅把它传入一次 SMTP 测试子进程，没有回显、持久化或修改全局变量。

QQ SMTP 选择不改变邮件状态语义：SMTP 受理与 Owner 收件箱实际可读必须分开；没有幂等/查询/回调证据时保持可追踪未知，不盲目重发、不伪造投递/退信回执或 exactly-once。2026-09-05 已向已确认的同一发件/收件邮箱发送 **1 封自测邮件**：TLS 1.3 且证书/主机名验证启用，AUTH 235、DATA 250。2026-09-06（Asia/Shanghai）Owner 明确回复“收件和中文展示确认没问题”，状态更新为 **SMTP 已接收，实际收件与中文显示已由 Owner 确认**；未重发，未读取邮箱。详见 [脱敏预检记录](acceptance/qq-smtp-preflight-2026-09-05.md)。这不完成 #19 产品实现/验收，也未启用日常自动投递。
