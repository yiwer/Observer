# QQ SMTP 单封授权预检

状态：**SMTP ACCEPTED / INBOX NOT CONFIRMED**。本记录不是 #19 产品投递器验收或生产发送资格。

Owner 已选择 QQ SMTP 授权码、发件与收件为同一邮箱，并明确允许使用本地 `QQ_SMTP_KEY` 测试。Root 发送且只发送一封标题为 `[Observer SMTP TEST] dd3b13d49618` 的测试邮件；正文为中文连通性说明，包含纯文本与 HTML 两种 MIME 表现，无新闻、链接、附件或真实日报。

## 实际观察

| 项目 | 结果 |
| --- | --- |
| 测试开始（UTC） | 2026-09-05T10:23:29.567670+00:00 |
| SMTP 接收（UTC） | 2026-09-05T10:23:30.338513+00:00 |
| 端点 | `smtp.qq.com:465`，隐式 TLS |
| TLS | 实际协商 TLS 1.3；最低允许 TLS 1.2；默认受信证书链与主机名验证 |
| EHLO / AUTH | 250 / 235 |
| MAIL / RCPT / DATA | 250 / 250 / 250 |
| QUIT / 子进程退出 | 221 / 0 |
| 测试编号 | `dd3b13d49618` |
| 收件箱及显示 | 待 Owner 人工确认，未查询 IMAP 或打开邮箱 |

## 凭证与操作边界

使用本机 Python 3.12.13 标准库 `smtplib` / `ssl` / `EmailMessage` 执行 ignored `data/operator/qq-smtp-probe.py`（实测脚本 SHA-256 `ac13a8121a20091a0bf5cc0f556b43cd3ef9ae6ddacd119bb247fd5f07751911`）。通过专有子进程的环境注入用户级授权码，不在命令行参数、源码、输出或证据中保存原值；子进程环境仅保留必要系统路径变量及本次输入，没有更改用户/系统环境变量。没有打印原始 SMTP 对话、服务器响应正文或异常正文。具体邮箱地址只在 ignored Owner 输入中，公开证据不包含地址。

脚本只有一次消息提交，没有发送重试；DATA 阶段连接结果未知时标记 `submission-unknown-do-not-retry`，不会自动重发。使用精确子进程句柄限制最长生命周期 60 秒，本次正常结束，未触发超时终止。

## 未覆盖

- DATA 250 只证明服务器受理，不证明进入收件箱、未被判为垃圾邮件或中文显示正常；已请求 Owner 检查本次唯一测试编号。
- 本次没有使用产品邮件适配器，也未经过 Canonical Markdown 转换、PDF 生成、附件上限、重试/去重、状态持久化或真正每日任务。
- 未验证目标 Linux/VPS 的网络、秘密注入、调度或邮件能力；本机一次成功不可外推。
- 未启用每日投递，未读取邮箱、购买服务或扩大 Provider 权限；后续 #19 / #26 仍按票逐项验收。
