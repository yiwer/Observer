# V1-22 快速交付接受记录

2026-09-08，按 Owner 快速 V1 策略接受并本地集成 `f7cd014`。作者 `/root/implement_v1_22`，worktree `O:/GenesisCode/Observer-worktrees/v1-22`，base `69f5d97`，功能 `aa6d1ff`，集中修复 `3143e19`。代码未 push，没有在 Owner 原数据库运行清理，也未调用真实来源、模型、SMTP 或部署。

## 交付与验证范围

接通原文/社交材料/更正候选的政策 TTL、30 天脱敏 Agent 事件、永久来源封禁及版本依赖闭包清理。删除标记先持久化，再清理报告正文、MD/PDF、冻结材料、关联任务和私有归档；旧版本不回退成较早可读版本，正文和下载返回 410。只保留政策允许的最小墓碑；邮件已受理/未知等去重身份及不可撤回范围保留，不承诺删除收件箱副本。

GitHub 超过 90 天的非发布依赖小时观察压缩为日观察，保留已发布榜单和动量/上下文所需输入、已报道身份与事件去重；仍被完整依赖链引用的小时数据不宣称已压缩。CLI 提供 run/status/remove/contract/apply-contract；#24 恢复须先应用最新外部删除契约及来源策略，再开放读取或任务。具体入口、秘密排除和保留取舍见[实施说明](../../../implementation/v1-22.md)。

作者功能块末类型检查通过；一次新建 SQLite 微型路径确认父子报告转墓碑、PDF 行清除、accepted 邮件身份保留/地址清空、不可撤回范围、重复应用契约及实际 latest 的 410。路径不代表正常日报发布、真实 GitHub 历史回放或节点外恢复。检查后补充的审计降级、候选身份清除、删除版本入队拒绝及 `3143e19` 三项修复均未动态重跑。

Root 仅阅读代码、最终窄差异及相关表列/调用；没有运行作者检查、hash 验收、全量测试、夹具矩阵或逐步/集成回归。未触碰历史样本与 `.idea/`。

## Standards

一次只读静态审查：1 项 P2 硬性问题，0 项需报告的启发式异味。retention 管理命令只检查 GitHub application_id，可能用旧清理程序修改未知新版存储，违反 ADR 0004。

集中修复在创建采集器和生命周期前同时检查 application_id 与 user_version=1。Root 静态核对关闭，未作动态复验。

## Spec

一次只读静态审查：2 项 P1 核心问题。缺失来源配置被当作权威空列表而永久删除历史报告；存在一次撤销后，每次维护会清空全部更正信号和未发布路由，误伤无关新任务。

集中修复保留“未提供配置”的 undefined：不推断撤销、不作依赖该策略的清理，需要策略的读取拒绝；明确空配置与显式删除契约仍有权威。任务清理沿来源、报告版本、任务和冻结 Bundle 依赖限定；未知依赖的保守兜底只固定首次撤销时已存在的任务身份，重试不纳入后来新任务，巡查材料按关联信号清理。Root 静态核对关闭。

Standards 原1、剩余0；Spec 原2、剩余0。最终修复只经静态核对，不声称测试通过。

## 未验证与交接

最终修复、真实定时/多日推进、GitHub 发布依赖回放、并发/崩溃恢复、节点外恢复、真实源/Provider/SMTP、客户端缓存删除和实际 Owner 数据清理未动态验证。开发票交付不等于生产或灾备验收；#23 接续单节点可运行部署，#24 接续一致性备份/恢复，#26 承接授权真实环境。
