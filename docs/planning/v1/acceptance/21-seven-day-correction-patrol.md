# V1-21 快速交付接受记录

2026-09-08，按 Owner 快速 V1 策略接受并本地集成 `bc7b558`。作者 `/root/implement_v1_21`，worktree `O:/GenesisCode/Observer-worktrees/v1-21`，base `1ca9e3d`，功能 `db308f0`，集中修复 `83f4a1f`。代码未 push，巡查默认关闭，没有真实来源、模型、SMTP 或部署操作。

## 交付与验证范围

按上海业务日 `[D−7,D)` 复查当前整刊的已发布陈述，追溯 Completion/Correction 的真实栏目来源；从已授权精确历史 URL 独立读取，不以普通 feed 轮询代替。持久每日任务、来源读取预算、有限重试、材料去重及可见缺口已经接通。既有 Provider 生成候选后仍须进入 #20 独立证据门；普通新进展材料交给下一次尚未冻结的常规选题，不直接发布。404/410、标题或时间变化不自动等于事实撤回。专用社交/GitHub 材料明确未覆盖。

默认06:00开始、07:15停止新巡查；自动更正07:15–08:30不领取，计划取消保留待续状态。独立 main tick 不等待巡查后才冻结或确认可读。配置、预算、状态命令和 #22 持久数据交接见[实施说明](../../../implementation/v1-21.md)。

作者在 `db308f0` 完成块末类型检查及唯一注入路径：从 #20 保存库创建 SQLite 备份副本，追溯当前 v3 与 AI 继承 v2，精确历史 URL 读取一次、候选研究一次，进入持久更正队列。首次类型收窄问题和检查脚本括号错误集中修正后通过；原样本未修改。该路径止于 queued，不代表最终语义发布、撤稿或发信。

`83f4a1f` 仅静态核对，没有重跑类型或动态路径。Root 读最终差异及相关调用，不运行作者检查、PDF 转换或集成回归。没有 hash 验收、新夹具、完整回归或扩展矩阵。

## Standards

一次只读静态审查：1 项硬性问题，0 项实质性代码异味。新动态 Source Policy 读取绕过 ADR 0005 的持久版本防回退规则。

集中修复使构造及动态读取共用 `admitPolicies`：同版本变文、版本回退拒绝，合法升级同事务推进最高版本记录；移除来源仍保留版本身份。Root 静态核对后关闭该发现。

## Spec

一次只读静态审查：1 项核心问题。07:15 计划取消误成永久 blocked，后续材料去重一直复用旧失败信号。修复仅将明确计划暂停恢复 pending，保留真实语义/策略/清理失败及显式冲突终态，并在发布事务及提交前检查截止。

Root 同批补充1项：旧冲突的材料身份会阻止新当前整刊再次检查仍有效的继承陈述。材料身份现包含已验证的当前版本；新日巡查仍须通过当前正文和来源检查，不修改或 rebase 旧冲突，也不恢复已移除陈述。

Standards 原1、剩余0；Spec 独立审查1加 Root补充1，剩余0。均依据最终代码静态核对关闭，未声称动态复验。

## 未验证与交接

最终修复、七日边界、多日变化、重启、来源收紧、并发/真实定时、普通新发展交接和下游最终语义发布/通知未动态验证。真实模型质量、来源网络、SMTP 收件及目标部署仍未实测；本票不代表完整 AC-13 或生产验收通过。

#22 负责现有及新增原文缓存、巡查元数据、候选队列与派生产物的到期/权利删除生命周期；不在本票执行历史清理。后续 #26 已单独对齐 Owner 快速策略及既有 QQ 预检，不再要求开发全量回归或误报授权码未提供。
