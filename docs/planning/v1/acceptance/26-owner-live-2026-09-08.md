# #26 实际本机补发：2026-09-08

Owner 已确认公开来源最少字段用途；批准记录见 [Owner 输入](../OWNER-INPUTS.md)。本记录覆盖旧 native 记录中“等待来源、无真实模型成功”的停止点，不替代完整发布验收。

## 实际生成

- 本机既有 Codex 登录，CLI 0.153.4，`gpt-6-astra / medium`。世界要闻与财经各一次 research、一次 verification，四次均 exit 0 / completed，进程退出。输入 token 合计 54844，输出 3950，费用 unknown/null；没有调用 Claude，没有声称双 Provider 验证或容器隔离通过。
- 正常来源采集 38 条（Eurostat 11、Fed 20、USGS 7）；按真实窗口筛出 8 条输入（Eurostat 1、USGS 7）。arXiv 两源没有可用条目；Fed 无窗口内条目；社交来源未配置。不是全球完整召回。
- GitHub 匿名公开元数据实际取得 `openai/codex` 一个仓库，冷启动观测，不是官方 Trending 或实测 24h 增长。
- `owner-2026-09-08-live` 的真实冻结为 `2026-09-08T08:02:36.861Z`，生成 `2026-09-08-v1` 于 `08:05:19.190Z`。标注 Owner 补发、迟于 08:30，没有伪造定时交付。
- 产物含六栏，但世界与财经 7 条陈述虽 semantic supported，仍全部被 `domain-assertion-inconsistent` 隔离；AI/前沿/社交为空，仅 GitHub 1 个 Watch Item。**内容明显不足，不视为日报内容验收通过。** 已发布的 Canonical 保持不可变，不另建数据库绕过同日初刊限制。

## 真实问题与局部处理

1. 先前两个 Owner 请求在模型调用前遭 SQLite `agent_events.id` 重复错误；保留失败记录。作者 `061b4b5` 修复外层 UPSERT 与旧 trigger 冲突，集成 `b3c267e`；后续实际链路完成四次模型调用和归档。不是合成测试结果。
2. 作者 `4acf91d` 补充 Claim/Assessment schema 的事实、归因、解释、basis 说明，独立静态 Standards/Spec 0，Root 读差异集成。不改枚举或发布门、不把隔离项手工改成通过。旧归档剥离了隔离项原始配对，无法证明具体 enum 错因；提示修正的实效留待后续运行。
3. PDF 共 5 页、149531 字节，从 Canonical MD 转换。已渲染逐页查看；Root 一度误判跨页段落侵入页脚，独立实际 bbox 确认第 2/3 页正文距页脚分别 44.44/50.87 pt，无重叠，不改 renderer。

遵从快速 V1：没有测试、夹具、build/typecheck、hash 验收或逐步回归。私人运行脚本、数据库、产物和账号均不提交；没有复制登录文件或输出 QQ 授权码。

## 邮件与未完成项

邮件采用单次 QQ SMTP、HTML 全文及完整 MD/PDF 附件，无虚构下载入口。请求 `owner-mail-2026-09-08-initial` 于 `08:12:23.233Z` 建账，`08:14:32.086Z` 返回 `accepted / smtp-accepted / 250`；Message-ID 为 `<bebd8179-6fb4-4809-b97d-5bd69d823165@observer.invalid>`。没有重试；SMTP 受理不等于收件箱送达，等待 Owner 确认本封收件、中文及附件。旧 Sep5 预检收件确认不能替代本封。

自动日程及后台副作用仍关闭。本次不关闭 #26；真实私有客户端访问、部署/灾备及其他 live 门尚未完成，#27 的 14 天批次和 #28 的人工审核不能由本次补发替代。源码本轮未 push。
