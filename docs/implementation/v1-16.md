# V1-16：降级、午前恢复与补齐

已接入 `main → production-runtime → Observer → SQLite → 私有 HTTP`。沿用 #15 的显式启用配置、来源授权、冻结材料、Provider 隔离、共享路由装配与清理熔断；默认不开启生产。

08:30 前可用内容正常发表，失败栏目保留 Coverage Gap。所有 Agent 不可用时，仅将当前政策允许永久归档的标题、来源署名、发布时间和链接投影到正文，不生成摘要或分析。社会与 GitHub 输入继续使用已有专用安全边界，不把原始帖子或仓库输入放入普通链接降级。无可信故事、合格 GitHub 元数据或合规链接时记录 `no-trustworthy-content`，不创建空正常刊或 outbox。

每个短重试周期仍由 `schedule.maxAttempts`（默认 3）限制，周期内使用 `retryDelayMs`；耗尽后按新增 `recoveryRetryDelayMs`（默认 5 分钟）重新尝试，整个恢复阶段受当日12:00硬截止约束。`attempts` 是累计实际尝试数。已发表缺栏的恢复采用相同低频间隔。进程退出后继续持久任务，发布与 outbox 保持同事务，重复无进展不会创建新版本。Agent 工作受08:30或午前阶段期限约束，并预留清理时间。

原冻结采集完全为空、尚未首发时，运行入口可从实际采集缓存补取原窗口 `(前日07:30, 当日07:30]` 内已公开的材料，保留真实迟到采集时间及 `collectionRecovery` 附加记录；时间未知、截稿后新披露不进入该次恢复。第一次补取后材料再次固定；Completion 不再吸收新采集。来源、配置、权限和原文有效期约束照常执行。

已发表缺栏恢复产生 `v2` 等 Completion Revision。只重新研究尚无有效内容的 Edition；本版完整 Canonical Markdown 继承前版其他栏目的原文字句，替换本次已恢复栏目，列出剩余缺口和前版本链接。每次必须至少恢复一个新栏目，因此同样恢复结果不会重复提交。旧 Report 永不改写；读取新版本也校验继承原文与前版本关系，并重新执行前版本当前权限检查。已有内容不足软目标不触发凑数重写。

12:00起事务拒绝恢复首发和补齐。从未发表的持久任务成为 `missed`；已有刊保留可读版本与缺口，恢复状态转 `closed`。后续 Correction / withdrawal 使用独立 `revisionWindowOpen` 入口，不受此午前限制；其证据核验和正式发布流程仍由 #17 对应工作实现，没有新增远程编辑入口。

私有 `GET /v1/reports/YYYY-MM-DD-vN` 与 `/markdown` 支持全部正整数版本。新增同一 Owner Bearer 鉴权的 `GET /v1/briefs/YYYY-MM-DD` 返回最新版本、实际完成/首次与最新可读时刻、内容状态、首发时效、恢复状态、失败原因和 Missed 时间。报告版本采用新增 schema 11；既有版本读取保留。`content` 与 `timing` 独立，正文显示实际发布时间，`onTime` 仍按首次私有读取探针记录，邮件 outbox 独立且不发送。

冻结运行版本新增 `recovery: observer-recovery-v1`。旧未完成任务若没有该运行版本，仍按原安全约定报告 `frozen-runtime-version-unavailable`；旧已发表版本仍可读。只创建当前业务日与继续已存在任务，不倒填没有冻结输入的离线历史日。

验证：`npm ci --ignore-scripts --offline --no-audit --no-fund` 成功；`npm run typecheck` 通过，首次发现的采集缺口原因类型过宽已改为通过原 RoutedRequest 契约解析。`node --test tests/recovery-publication.test.ts` 通过 1/1，覆盖真实 SQLite/私有HTTP、链接版、恢复首发过滤截稿后新新闻、迟到状态、后续版本读取、无重复研究/投递事件、重启与午间关闭；Provider/来源响应是本地替身。

依 Owner 快速交付策略，未跑全量 check/smoke、旧版本矩阵、Docker、真实 Provider、GitHub/Mastodon、SMTP、部署或人工版式验收；无开发验收 hash/capture/manifest。类型检查与核心路径不代表真实内容质量或生产准时率。
